// Root e2e entry point — actual tests live in test/e2e/
// This file exists to satisfy Jest's "must contain at least one test" requirement.

it('e2e suite loaded', () => {
  expect(true).toBe(true);
});
